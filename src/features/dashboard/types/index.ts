/**
 * Dashboard Types.
 * Defines TypeScript interfaces and types for the dashboard feature.
 */

export interface DashboardModule {
  id: string;
  title: string;
  description: string;
  iconType?: string;
}

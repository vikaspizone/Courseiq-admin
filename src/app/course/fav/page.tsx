/**
 * Course Favorites Page.
 * Renders the course favorites view for the application.
 */
import { CourseFavView } from '@/features/course/screen/CourseFavView';

export default function CourseFavPage() {
  return <CourseFavView />;
}
